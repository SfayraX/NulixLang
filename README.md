# Nulix — Универсальный многоуровневый язык программирования

## Один язык от скриптов до драйверов. От прототипа — к продакшену. От Python — к ASM.

Nulix — экспериментальный язык нового поколения, основанный на уникальной архитектурной модели:

### **Уровни абстракции:**
- **@high** — Python‑стиль, высокая продуктивность
- **@system** — Rust‑стиль, безопасность и контроль
- **@low** — C‑стиль, указатели и ручная память
- **@naked** — ASM‑стиль, машинный код

### **Уровни строгости:**
- **@prototype** — свобода и быстрые эксперименты
- **@develop** — мягкая типизация и предупреждения
- **@production** — строгая безопасность и Result‑модель
- **@strict** — максимальная детерминированность

**Главная идея Nulix:**
> Один код может эволюционировать от быстрых скриптов до высокопроизводительных системных модулей — без переписывания.

---

# ✨ Почему Nulix

## 🔹 1. Четыре уровня мощности в одном языке
Nulix позволяет писать:
- высокоуровневые сервисы
- строгие системные модули
- low‑level HFT и embedded код
- ASM‑участки для оптимизаций

Все это — в одном файле, с одним синтаксисом.

## 🔹 2. Постепенное ужесточение
Начинай как Python:
```
@prototype proc calc(a, b):
    a + b
```

Заканчивай как Rust:
```
@production proc calc(a: f64, b: f64) -> Result[f64, Error]:
    Ok(a + b)
```

## 🔹 3. Автоматические переходы между мирами
```
@high proc trading_bot:
    let sig = @system analyze(data)   // high → system
    let r   = @low execute(sig)       // system → low
```

Компилятор сам добавляет проверки, границы и безопасные обёртки.

## 🔹 4. Акторы и предсказуемый runtime
Как Elixir/BEAM + реальное время + CPU‑классификация:
- @light — I/O bound
- @burst — CPU ≤ 200 μs
- @heavy — долгие задачи
- @pinned — realtime
- @auto — компилятор решает сам

---

# 🚀 Пример Nulix «снизу вверх»

## **1) Высокоуровневый код**
```
@high proc main:
    let grid = TradingGrid("XAUUSD")
    await grid.run()
```

## **2) Системный слой**
```
@system proc analyze_tick(tick: &Tick) -> Action:
    if tick.price > avg:
        Buy
    else:
        Sell
```

## **3) Низкоуровневый код**
```
@low proc exec(order: *Order) -> i32:
    unsafe:
        return extern "C" send_order(order)
```

## **4) Голое железо**
```
@naked proc atomic_inc:
    asm:
        lock inc [counter]
```

---

# 🧠 Архитектура языка

## **Компилятор**
- Парсер + AST
- Система уровней абстракции
- Механизм gradual hardening
- Lowering (high → system → low → naked)
- LLVM backend (AOT и JIT)

## **Runtime**
- акторная модель
- 4 независимых планировщика
- pinned heap для realtime
- thread‑local allocators

## **Модель памяти**
- безопасность без GC
- владение и заимствования
- auto-lifetimes

---

# 📦 Roadmap Nulix (2025–2027)

## **v0.1 — MVP parser** *(готово)*
- базовый синтаксис
- AST

## **v0.2 — архитектурная спецификация** *(готово)*
- уровни абстракций
- уровни строгости
- модель lowering
- формирование языка

## **v0.3 — Type system & Strict mode**
- типы
- generics
- traits
- Result‑модель

## **v0.4 — Компилятор (system → IR → LLVM)**
- codegen
- lowering pipeline

## **v0.5 — Акторы и runtime**
- @light / @burst / @heavy / @pinned

## **v0.6 — JIT REPL**
- быстрые эксперименты

## **v0.7 — WASM runtime**
- фронтенд без JS

## **v1.0 — Язык готов**
- стабильная спецификация
- продакшен‑готовый компилятор

---

# 🛠 Сборка MVP
```
git clone https://github.com/yourname/NulixLang
cd NulixLang
mkdir build && cd build
cmake ..
cmake --build . --config Debug
```

Запуск теста:
```
Get-Content ..\test.nx | .\Debug\nulix_compiler.exe
```

---

# 📄 Лицензия
MIT — используй, форкай, дополняй.

Nulix создаётся для тех, кто устал от языков, заставляющих выбирать между удобством и скоростью.

